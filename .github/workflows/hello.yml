name: Hello World CI

on:
  push:
    branches:
      ["master"]
  pull_request:
    branches:
      ["master"]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print a message
        run: echo "HELLO GITHUB WORLD!"